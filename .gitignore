# Rust build artifacts
/target/

# Backtraces
rustc-ice-*.txt

# Editor / OS noise
*.swp
.DS_Store

# Agent session / runtime artifacts — never commit these onto audit branches.
.claude/runtime/
.claude/**/runtime/
